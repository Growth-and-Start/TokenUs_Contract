{
  "name": "tokenus",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "bootstrap": "pnpm -r install",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm --filter @tokenus/examples-minimal-dapp dev"
  },
  "workspaces": [
    "packages/*",
    "packages/examples/*"
  ]
}
